<!--
  TEMPLATE — copy this to `<yourname>-field-notes.md` at the repo root and
  `git add` it so it is tracked. This file DOES get committed — it is how your
  onboarding journey reaches the maintainer.

  Your AI assistant appends dated entries here automatically as you hit friction
  (see CLAUDE.local.md). You can also write in it yourself anytime.
-->

# <yourname> — Bloob Haus onboarding field notes

This is my running log of building my own Bloob Haus site: what I had to change, what confused me, what
didn't work, and what I wished "just worked." It's raw and append-only — **not** a polished doc. The
maintainer reads it to make onboarding smoother for the next person.

Newest entries at the top.

---

## (example entry — delete once you have real ones)

### 2026-01-01 — had to rename image files to avoid a collision
Two notes both embedded `![[diagram.png]]` but they were different images in different folders. Only
one showed up on the site. Renamed them to unique names and both worked. Would've expected folder-based
embeds to disambiguate. (See onboarding `prepare-your-vault.md` §4 — turns out this is a known sharp
edge; noting it because I hit it anyway.)
